import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.test.matchmaker',
  appName: 'MatchMaker',
  webDir: 'dist/client-app/browser'
};

export default config;
